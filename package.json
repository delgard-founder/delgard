{
  "name": "delgard-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "demo": "pnpm build && pnpm --filter two-agent-demo start"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "@types/node": "^20.14.0"
  },
  "packageManager": "pnpm@9.0.0"
}
